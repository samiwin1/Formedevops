apiVersion: v1
kind: Service
metadata:
  name: userservice
  namespace: forme-education
  labels:
    app: userservice
spec:
  type: ClusterIP
  selector:
    app: userservice
  ports:
    - name: http
      port: 80
      targetPort: 8081
